<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sf.DarkCalculator">

    <application
        android:icon="@mipmap/launch"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".interestActivity"
            android:exported="true"
            android:parentActivityName=".MoreFunctionActivity"/>
        <activity
            android:name=".exchangeActivity"
            android:exported="true"
            android:parentActivityName=".MoreFunctionActivity"/>
        <activity
            android:name=".MoreFunctionActivity"
            android:exported="true"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ResultsActivity"
            android:label="运算结果" /> <!-- <activity -->
        <!-- android:name=".CapitalMoneyActivity" -->
        <!-- android:label="大写数字" -->
        <!-- android:screenOrientation="portrait" /> -->
        <!-- <activity -->
        <!-- android:name=".BigDecimalActivity" -->
        <!-- android:label="大数运算" -->
        <!-- android:screenOrientation="portrait" /> -->
        <activity
            android:name=".AboutActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>